apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "yttscrb.fullname" . }}-app
spec:
  replicas: {{ .Values.app.replicas }}
  selector:
    matchLabels:
      app.kubernetes.io/name: {{ include "yttscrb.fullname" . }}-app
  template:
    metadata:
      labels:
        app.kubernetes.io/name: {{ include "yttscrb.fullname" . }}-app
    spec:
      {{- with .Values.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      containers:
        - name: app
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          command: {{- toYaml .Values.app.command | nindent 12 }}
          args: {{- toYaml .Values.app.args | nindent 12 }}
          ports:
            - containerPort: 80
          envFrom:
            - configMapRef:
                name: {{ include "yttscrb.fullname" . }}-config
            - secretRef:
                name: {{ .Values.app.secrets.existingSecret }}
          volumeMounts:
            - name: cookies
              mountPath: /app/cookies
              readOnly: true
      volumes:
        - name: cookies
          secret:
            secretName: {{ .Values.app.secrets.existingSecret }}
            items:
              - key: YT_DLP_COOKIES
                path: cookies.txt
            optional: true
