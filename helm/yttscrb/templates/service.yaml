apiVersion: v1
kind: Service
metadata:
  name: {{ include "yttscrb.fullname" . }}-app
spec:
  type: ClusterIP
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 80
      protocol: TCP
      name: http
  selector:
    app.kubernetes.io/name: {{ include "yttscrb.fullname" . }}-app
